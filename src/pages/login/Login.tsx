import React from "react";

export default class Login extends React.Component {
    render(): React.ReactNode {
        return(
            <>
                로그인 이동
            </>
        )
    }
}